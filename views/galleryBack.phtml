<main class="galleryBack">
  <h1>Gallery</h1>

  <?php if (isset($_GET["errorMsg"])) : ?>
  <h3><?= htmlspecialchars($_GET["errorMsg"]) ?></h3>
  <?php endif; ?>
  <form action="uploadPhoto" method="post" enctype="multipart/form-data">
    <legend>Create a post</legend>
    <!-- <input type="file" name="img"> -->
    <input name="name" placeholder="Name"></input>
    <textarea name="caption" cols="30" rows="3" placeholder="Caption"></textarea>
    <label>Date</label>
    <input name="date" type="date"></input>
    <!-- <select name="location_id"> -->
    <option selected disabled hidden>Choose a location</option>
    <option value="1">Fuvahmulah Atoll</option>
    <option value="1">Make this dynamic</option>
    </select>
    <input type="file" name="img">
    <button type="submit" name="submit">CONFIRM</button>
  </form>


  <div class="photos-container">
    <?php foreach ($photos as $photo): ?>
    <figure>

      <h2><?= htmlspecialchars($photo['name']) ?></h2>
      <img src="assets/img/<?= htmlspecialchars($photo['src']) ?>" alt="<?= htmlspecialchars($photo['name']) ?>"
        width="300">
      <h3><em><?= htmlspecialchars($photo['caption']) ?></em></h3>
      <p><?= htmlspecialchars($photo['location_id']) ?>, <?= htmlspecialchars($photo['date']) ?></p>
    </figure>

    <?php endforeach ?>
  </div>
</main>