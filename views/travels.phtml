<main class="travels">
  <h1>Travels</h1>
  <div class="container">
    <?php foreach ($posts as $post): ?>
    <?php if ($post['display'] == 'show'): ?>
    <article>
      <a href="post-id-<?= htmlspecialchars($post['id']) ?>">
        <img src="assets/img/<?= htmlspecialchars($post['src1']) ?>">
      </a>
      <div class="text">
        <h2><a href="post-id-<?= htmlspecialchars($post['id']) ?>"><?= htmlspecialchars($post['name']) ?></a></h2>
        <p><a href="post-id-<?= htmlspecialchars($post['id']) ?>"><?= htmlspecialchars($post['text1']) ?></a></p>
        <p class="readMore"><a href="post-id-<?= htmlspecialchars($post['id']) ?>"><i
              class="fas fa-long-arrow-alt-right"></i>Dive Deeper</a></p>
      </div>
    </article>
    <?php endif; ?>
    <?php endforeach; ?>
  </div>
</main>